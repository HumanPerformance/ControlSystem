import bluetooth

nearby_devices = bluetooth.discover_devices(lookup_names=True)
print("found %d devices" % len(nearby_devices))

addr_one = "00:06:66:8C:9C:2E"

for addr, name in nearby_devices:
	
	print("  %s - %s" % (addr, name))
